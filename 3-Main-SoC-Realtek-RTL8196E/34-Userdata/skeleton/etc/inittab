# /userdata/etc/inittab - GENERATED by build_userdata.sh; do not edit.
# Source of truth: 33-Rootfs/skeleton/etc/inittab with the ::sysinit
# entry stripped (a sysinit action here would re-run rcS on every
# SIGHUP).  init runs on built-in fallback defaults until
# S99enablealtuart0 re-reads this table (kill -HUP 1).
#
# Alt-uart0 mode (external device on ttyS0) = the getty line below
# commented out with the #[altuart0] marker - see altuart0.common.
ttyS0::respawn:/sbin/getty -L 38400 ttyS0 vt100
::shutdown:/userdata/etc/init.d/rcK
